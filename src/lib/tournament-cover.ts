/** Shared tournament cover crop / display constants (client-safe). */

/** Matches the Our Tournaments featured card aspect ratio. */
export const TOURNAMENT_COVER_ASPECT = 2.2;

/** Output width for cropped covers (height = width / aspect). */
export const TOURNAMENT_COVER_OUTPUT_WIDTH = 1760;

export const TOURNAMENT_COVER_ASPECT_LABEL = "2.2∶1";
